{
  "name": "express-app",
  "version": "1.0.0",
  "description": "Minimal Express app fixture for L3-001",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --silent"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["<rootDir>/tests/**/*.test.js"]
  }
}
