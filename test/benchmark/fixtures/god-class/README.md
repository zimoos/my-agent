# god-class

This fixture contains a single `Manager` class that mixes IO, business logic, and
output formatting. The refactor task asks for splitting it into cohesive modules
while keeping `npm test` green.
